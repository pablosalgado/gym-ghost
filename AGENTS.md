# Agents

All project instructions live in `.github/copilot-instructions.md`. Read that file for branch workflow, conventions, anti-patterns, and commands.

This file exists so agents that auto-detect `AGENTS.md` know where to find the real instructions.
