#!/bin/bash -e

if [ "$(id -u)" = "0" ]; then
  chown -R app:app /app/storage
  exec gosu app "$0" "$@"
fi

# If running the rails server then create or migrate the persistent database.
if [ "$1" = "./bin/rails" ] && [ "$2" = "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
